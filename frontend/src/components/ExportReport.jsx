import { useState } from "react";
import axios from "axios";
import generatePDF from 'react-to-pdf';

const ExportReport = ({ setIsCreateFormOpen, handleCreateReportSuccess }) => {
  
};

export default ExportReport;
